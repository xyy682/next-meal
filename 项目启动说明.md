# Diet Agent 项目启动说明

## 一、运行结构

本项目采用以下运行方式：

```text
Windows
├── Spring Boot（Java 21，端口 8080）
└── Docker Desktop / WSL 2
    └── MySQL 8.4 容器（Windows 端口 3307）
```

- 项目目录：`D:\学习项目\diet-agent`
- 项目页面：http://localhost:8080
- Docker MySQL：`localhost:3307`
- Windows 本机 MySQL：`localhost:3306`，本项目不使用
- Docker 数据实际存放于 D 盘的 Docker 虚拟磁盘
- Maven 依赖实际存放于 `D:\MavenRepository`

## 二、每次启动项目

### 第一步：启动 Docker Desktop

从 Windows 开始菜单启动 Docker Desktop，等待 Docker Engine 就绪。

如果 Docker Desktop 界面白屏，只要下面的命令能够正常返回，Docker Engine 仍可以使用：

```powershell
docker info
```

### 第二步：启动 MySQL 容器

打开 PowerShell，执行：

```powershell
cd "D:\学习项目\diet-agent"
docker compose up -d
docker compose ps
```

看到类似以下状态表示数据库正常：

```text
diet-agent-mysql   Up ... (healthy)
```

数据库连接信息：

```text
地址：localhost
端口：3307
数据库：diet_db
用户名：root
密码：123456
```

### 第三步：在 IntelliJ IDEA 中启动 Spring Boot

1. 使用 IDEA 打开 `D:\学习项目\diet-agent`。
2. 等待 Maven 项目加载完成。
3. 打开 `src/main/java/com/diet/DietApplication.java`。
4. 点击类旁边的绿色三角形。
5. 选择 `Run 'DietApplication'`。
6. 等待控制台出现：

```text
Tomcat started on port 8080
Started DietApplication
```

### 第四步：访问项目

浏览器打开：

```text
http://localhost:8080
```

## 三、停止项目

### 停止 Spring Boot

在 IDEA 的 Run 窗口中点击红色停止按钮。

### 停止 MySQL 容器

在 PowerShell 中执行：

```powershell
cd "D:\学习项目\diet-agent"
docker compose stop
```

此操作不会删除数据库数据。下次执行 `docker compose up -d` 即可恢复运行。

也可以删除容器但保留数据库卷：

```powershell
docker compose down
```

## 四、常用检查命令

检查 Docker 是否正常：

```powershell
docker info
```

检查 MySQL 容器：

```powershell
cd "D:\学习项目\diet-agent"
docker compose ps
```

查看 MySQL 容器日志：

```powershell
docker compose logs mysql --tail 100
```

检查 Spring Boot 页面：

```powershell
Invoke-WebRequest -UseBasicParsing http://localhost:8080
```

## 五、常见问题

### 1. 提示 8080 端口被占用

说明已经有一个 Spring Boot/Java 进程正在运行。不要重复启动；先在原来的 IDEA Run 窗口中停止它。

检查端口：

```powershell
Get-NetTCPConnection -LocalPort 8080 -State Listen
```

### 2. 提示无法连接 localhost:3307

先确认 Docker Desktop 已启动，再执行：

```powershell
cd "D:\学习项目\diet-agent"
docker compose up -d
docker compose ps
```

### 3. IDEA 中 Maven 依赖标红

打开 IDEA 右侧 Maven 面板，点击 `Reload All Maven Projects`。

Maven 默认路径 `C:\Users\28226\.m2\repository` 已设置为目录联接，真实依赖保存在：

```text
D:\MavenRepository
```

不要删除这两个路径中的任何一个。

### 4. Docker Desktop 白屏

这通常是 Docker Desktop 的 Electron 图形界面异常，不代表 Docker Engine 或数据库损坏。可以先用 `docker info` 和 `docker compose ps` 判断后台是否正常。

如需重启 Docker Desktop，使用系统托盘正常退出后重新打开。不要执行 `Clean / Purge data` 或 `Reset to factory defaults`。

## 六、数据安全注意事项

不要随意执行：

```powershell
docker compose down -v
```

其中 `-v` 会删除本项目的 MySQL 数据卷。只有确定需要清空数据库并重新执行初始化 SQL 时才能使用。

## 七、当前尚未完成的功能

项目原版使用阿里云 DashScope 的 `DashScopeChatModel` 和通义千问模型。现有 DeepSeek API Key 不能直接填写到 `agentscope.dashscope.api-key`。

目前已正常工作的部分：

- Spring Boot 启动
- 首页访问
- Docker MySQL
- 数据库初始化
- 普通数据库接口

大模型聊天功能需要完成 DeepSeek 模型适配后才能使用。
