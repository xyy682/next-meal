<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.diet.mapper.SlotOptionMapper">
    <select id="findEnabledValues" resultType="java.lang.String">
        SELECT option_value
        FROM diet_slot_option
        WHERE slot_name = #{slotName}
        AND enabled = 1
        ORDER BY sort_order ASC, id ASC
    </select>
    <select id="findSlotNames" resultType="java.lang.String">
        SELECT slot_name
        FROM diet_slot_option
        WHERE enabled = 1
        GROUP BY slot_name
        ORDER BY MIN(sort_order), slot_name
    </select>
</mapper>
